<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      PixelTransfer moves files over an optical link only: one screen, one camera. CAMERA is the
      only platform permission it asks for - no INTERNET, no ACCESS_NETWORK_STATE, no Bluetooth,
      no NFC, no location - which is what makes the "no internet" property verifiable from the
      built APK rather than merely claimed.

      androidx.core additionally injects a signature-level permission of its own
      (${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION) to guard non-exported dynamic
      broadcast receivers on Android 13+. It is self-defined and grants nothing outside the app.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixelTransfer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PixelTransfer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Accept a file shared from another app as the transfer source. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>
    </application>
</manifest>
